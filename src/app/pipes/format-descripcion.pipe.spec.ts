import { FormatDescripcionPipe } from './format-descripcion.pipe';

describe('FormatDescripcionPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDescripcionPipe();
    expect(pipe).toBeTruthy();
  });
});
